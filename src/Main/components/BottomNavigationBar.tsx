import IconButton from "./BottomNavigationBar/IconButton";
import * as Styled from "../css/bottom.navigation.bar.styled";

export default function BottomNavigationBar() {

    return (
        <Styled.NavigationBar>
            <IconButton />
        </Styled.NavigationBar>
    );
}